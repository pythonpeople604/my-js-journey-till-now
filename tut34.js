console.log('hello');

setTimeout(() => {
    for (let index = 0; index < index < 45; index++) {
        const element = index;
        console.log('this is index number' + index);
        
    }
    
}, 100);

for (let index = 0; index < index < 45; index++) {
    const element = index;
    console.log('this is index number' + index);
    
}
console.log('done printing');
